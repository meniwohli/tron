{% extends "layout.twig" %}

	{% block body %}
		{{ parent() }}
		
		{# Aufforderung löschen bestätigen #}
		
		{% if geloescht %}
			Platz wurde aus der Datenbank gel&ouml;scht.
		{% else %}
			<div>
				Wenn Sie mit l&ouml;schen best&auml;tigen, wird der Platz unwiederruflich gel&ouml;scht!
				</br>
				Sind Sie sicher?
				</br>
				</br>
				<form method="post" action="../php/platzloeschen.php">
   		    		<input type="hidden" name="{{ pid }}">
   		    		<input type="hidden" name="bestaetigt">
					<input type="submit" value="Platz l&ouml;schen">
				</form>
			</div>
		{% endif %}
		
	{% endblock %}
