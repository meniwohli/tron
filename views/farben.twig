{% extends "layout.twig" %}

	{% block body %}
		{{ parent() }}
		
		<div>Farben</div>
		
		
		{# Farben auswählen #}
		</br>
		</br>
		Sie k&ouml;nnen folgende Farben ausw&auml;hlen:
		</br>
		
		<form method="post" action="../php/farben.php">
			Platz frei:
			<select name="frei" >
				{% for key, value in farbarray %}
				<option style="background-color:#{{ key }}">{{ value }}</option>
				{% endfor %}
			</select>
			</br>
			</br>
			
			Platz besetzt:
			<select name="besetzt" >
				{% for key, value in farbarray %}
				<option style="background-color:#{{ key }}">{{ value }}</option>
				{% endfor %}
			</select>
			</br>
			</br>
			
			Platz gesperrt/geschlossen:
			<select name="zu" >
				{% for key, value in farbarray %}
				<option style="background-color:#{{ key }}">{{ value }}</option>
				{% endfor %}
			</select>
			</br>
			</br>
			
			Serien:
			<select name="geschlossen" >
				{% for key, value in farbarray %}
				<option style="background-color:#{{ key }}">{{ value }}</option>
				{% endfor %}
			</select>
			</br>
			</br>
			
			Turnier:
			<select name="geschlossen" >
				{% for key, value in farbarray %}
				<option style="background-color:#{{ key }}">{{ value }}</option>
				{% endfor %}
			</select>
			</br>
			</br>
		</form>
		
		
	{% endblock %}
